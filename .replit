# Assigning the variables
myCurrentAge = 21  # Replace 25 with your current age
currentYear = 2003  # The current year

# Print the variables to ensure the output is correct
print("My current age is:", myCurrentAge)
print("The current year is:", currentYear)
